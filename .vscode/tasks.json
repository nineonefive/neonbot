{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "loadDiscordToken",
            "type": "shell",
            "command": "source",
            "args": [
                "${workspaceFolder}/.discord"
            ]
        }
    ]
}